load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "informativedrawings",
    srcs = ["qoi.zig"],
    main = "main.zig",
    deps = [
        "@zml//bazel",
        "@zml//zml",
    ],
)
